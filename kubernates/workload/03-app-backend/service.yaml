apiVersion: v1
kind: Service
metadata:
  name: matchzone-be
  namespace: app
  labels:
    app: matchzone-be
spec:
  selector:
    app: matchzone-be
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
