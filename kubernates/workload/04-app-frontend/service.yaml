apiVersion: v1
kind: Service
metadata:
  name: matchzone-fe
  namespace: app
  labels:
    app: matchzone-fe
spec:
  selector:
    app: matchzone-fe
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
